package com.oguzhandongul.locationtrackingsdk

import android.content.Context
import com.oguzhandongul.locationtrackingsdk.core.models.SdkConfig

object LocationSdk {
    fun initialize(context: Context, config: SdkConfig) {
        // Initialize modules here
        //TODO PermissionManager.initialize(context)
        //TODO LocationManager.initialize(context, config)
        //TODO NetworkManager.initialize(config)
    }
}